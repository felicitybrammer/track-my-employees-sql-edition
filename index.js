const inquirer = require('inquirer');

const questions = require('./questions'); //check file path if move
//require connection
//require handlers 

function start() {
    //get questions then take answers, invoke handler functions to perform action, then start
};


start();